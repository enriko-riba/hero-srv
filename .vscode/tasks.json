{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/ws-hero-srv.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}